# API Configuration
# Local development (tool-service running via `poetry ivcap run -- --port 8078`)
# VITE_API_URL=http://localhost:8078

# Deployed IVCAP platform (uncomment to use)
# VITE_API_URL=https://develop.ivcap.net

# Auth for client -> IVCAP Jobs API
# Backend uses IVCAP_JWT for LiteLLM proxy auth.
# VITE_AUTH_TOKEN=your-bearer-token-here

# VITE_SERVICE_URN = urn:ivcap:service:foo

# LiteLLM proxy base URL used by chat demo docs/config.
# Actual proxy requests are sent by backend using LITELLM_PROXY + bearer token.
VITE_LITELLM_PROXY=https://mindweaver.develop.ivcap.io/litellm
